# GBA 에뮬레이터 제작 지식 정리

실제로 동작하는(연구용/포트폴리오용이 아닌) GBA(Game Boy Advance) 에뮬레이터를 만들기 위해 필요한 기초 지식을 GBATEK(problemkaputt.de/gbatek.htm) 기준으로 정리한 문서. 이후 NDS 에뮬레이터로 확장할 계획이라, 여기서는 GBA 범위(ARM7TDMI 단일 코어)로 한정한다.

**이 문서 읽는 법**: 전문 용어는 처음 나올 때만 `용어(원어, 짧은 뜻)` 형태로 괄호 설명을 붙이고, 이후로는 용어만 그대로 쓴다. 전체 용어는 맨 끝 [용어집](#14-용어집-glossary)에서 다시 찾아볼 수 있다.

---

## 목차

0. [시스템 개요](#0-시스템-개요)
1. [CPU (ARM7TDMI)](#1-cpu-arm7tdmi)
2. [메모리 맵](#2-메모리-맵)
3. [인터럽트](#3-인터럽트)
4. [타이머](#4-타이머)
5. [DMA](#5-dma)
6. [그래픽 (PPU)](#6-그래픽-ppu)
7. [사운드](#7-사운드)
8. [키패드 입력](#8-키패드-입력)
9. [시리얼 통신 (SIO) — 링크케이블](#9-시리얼-통신-sio--링크케이블)
10. [BIOS / SWI 함수](#10-bios--swi-함수)
11. [카트리지 세이브](#11-카트리지-세이브)
12. [카트리지 헤더](#12-카트리지-헤더)
13. [에뮬레이터 설계 실무](#13-에뮬레이터-설계-실무)
14. [용어집 (Glossary)](#14-용어집-glossary)

---

## 0. 시스템 개요

GBA는 **ARM7TDMI** CPU 하나로 CPU와 그래픽/사운드 컨트롤 전부를 처리하는 단일 코어 시스템이다(NDS처럼 ARM9+ARM7 듀얼 구조가 아님 — 이 부분이 GBA를 먼저 만드는 게 합리적인 이유).

- **클럭**: 16.78MHz (정확히는 2²⁴ Hz = 16,777,216Hz). 이 숫자가 타이머/사운드/DMA 타이밍 계산에 계속 등장하니 외워두면 좋음
- **주요 구성**: CPU(ARM7TDMI) + 메모리 + PPU(그래픽 처리 담당 회로 — GBA엔 별도 이름이 없고 "LCD 컨트롤러"라고도 부름) + APU(사운드 처리 회로) + DMA 컨트롤러 + 타이머 + SIO(시리얼 통신)
- **부팅 흐름**: 전원 On → 내장 BIOS(16KB ROM)가 초기화 및 무결성 체크 → 카트리지 ROM의 엔트리포인트로 점프 → 게임 실행
- **화면**: 240×160 픽셀, 15비트 컬러(RGB555, 채널당 5비트)
- **레지스터 규칙**: I/O 레지스터는 `0x04000000`부터 시작하는 고정 주소 공간에 매핑되어 있고, 이 문서 전체에서 나오는 `DISPCNT`, `IE` 같은 이름들은 전부 이 공간의 특정 주소를 가리키는 별명(mnemonic)이다.

## 1. CPU (ARM7TDMI)

### 1.1 개요

**ARM7TDMI**는 32비트 RISC(Reduced Instruction Set Computer, 명령어를 단순화해서 하나하나는 빠르게 처리하는 설계 철학) CPU 코어. 이름 자체가 스펙을 담고 있음:

- **ARM7**: ARM사의 7번째 코어 세대
- **T**humb: 16비트 압축 명령어 세트 지원
- **D**ebug: 하드웨어 디버그 기능 내장
- **M**ultiplier: 전용 곱셈기 내장(빠른 32×32 곱셈)
- **I**: 임베디드 ICE(In-Circuit Emulator, 브레이크포인트 등을 지원하는 내장 디버깅 회로)

GBA에서는 16.78MHz로 동작. NDS의 ARM7TDMI(서브 CPU, 33MHz)와 같은 코어지만, NDS의 ARM9EJS(메인 CPU)는 완전히 다른 코어라는 점을 나중에 NDS로 넘어갈 때 기억해둘 것.

### 1.2 레지스터 파일

범용 레지스터 16개(R0~R15) + 상태 레지스터로 구성:

- **R0~R12**: 범용, 특별한 용도 규약 없음(단 R11은 컴파일러에 따라 프레임 포인터로 관례적 사용)
- **R13 (SP, Stack Pointer)**: 스택 포인터. 관례일 뿐 하드웨어가 강제하진 않지만 거의 모든 코드가 이렇게 씀
- **R14 (LR, Link Register)**: 함수 호출 시 복귀 주소가 자동으로 여기 저장됨(`BL` 명령어), 예외 진입 시에도 복귀 주소가 여기 저장됨
- **R15 (PC, Program Counter)**: 다음 실행할 명령어 주소. 다른 레지스터와 달리 읽을 때 파이프라인 오프셋(+8/+4)이 붙고, 쓰면 분기가 일어남

**뱅킹(Register Banking)**: CPU 모드가 바뀌면 R8~R14 중 일부가 "그 모드 전용 사본"으로 자동 교체된다. 이건 예외 처리 속도를 위한 설계 — 예외 핸들러가 매번 레지스터를 메모리에 save/restore 안 해도 되게.

| 모드 | 뱅킹되는 레지스터 |
|---|---|
| User/System | 뱅킹 없음(기본 R8~R14 그대로) |
| FIQ | R8~R14 전부 뱅킹(7개, 가장 많음 — FIQ는 최고 우선순위 인터럽트라 컨텍스트 스위칭 자체를 최소화하려는 설계) |
| IRQ | R13, R14만 뱅킹 |
| Supervisor(SWI) | R13, R14만 뱅킹 |
| Abort | R13, R14만 뱅킹(GBA에선 실질적으로 거의 안 씀 — 메모리 어보트 자체가 드묾) |
| Undefined | R13, R14만 뱅킹(정의 안 된 명령어 실행 시) |

즉 FIQ를 제외한 모든 예외 모드는 "이 모드 전용 스택(R13)"과 "이 모드 전용 복귀주소(R14)"만 따로 갖고, R0~R12는 이전 모드와 공유한다.

### 1.3 프로세서 모드

CPSR 하위 5비트(M4~M0)로 현재 모드를 나타냄:

| 비트값(M4~M0) | 모드 | 진입 상황 |
|---|---|---|
| `10000` | User | 일반 애플리케이션 실행(권한 없음, SPSR 접근 불가) |
| `10001` | FIQ | Fast Interrupt 발생 시 |
| `10010` | IRQ | 일반 Interrupt 발생 시(GBA에서 가장 흔하게 쓰이는 예외 모드) |
| `10011` | Supervisor | 리셋 시, 또는 `SWI` 명령어 실행 시(BIOS 호출) |
| `10111` | Abort | 잘못된 메모리 접근 시 |
| `11011` | Undefined | 정의되지 않은 명령어 실행 시 |
| `11111` | System | User와 권한은 같지만 뱅킹된 레지스터 접근 가능(주로 OS 커널 코드용, GBA 게임은 잘 안 씀) |

### 1.4 CPSR / SPSR 비트 배치

**CPSR(Current Program Status Register)**은 레지스터가 아니라 "지금 CPU 상태"를 담은 32비트 상태값이다. 비트 배치:

| 비트 | 이름 | 의미 |
|---|---|---|
| 31 | N | Negative, 연산 결과가 음수(최상위 비트=1)면 설정 |
| 30 | Z | Zero, 연산 결과가 0이면 설정 |
| 29 | C | Carry, 자리올림/빌림 발생 여부(덧셈이면 캐리아웃, 뺄셈이면 "빌림 없음"이 1) |
| 28 | V | Overflow, 부호있는 연산에서 오버플로우 발생 시 설정 |
| 27~8 | (예약) | GBA/ARM7TDMI에선 미사용 |
| 7 | I | IRQ 비활성화(1이면 IRQ 무시) |
| 6 | F | FIQ 비활성화(1이면 FIQ 무시, GBA BIOS는 FIQ를 아예 안 씀) |
| 5 | T | Thumb 상태 여부(1=Thumb, 0=ARM) — `BX` 명령어로만 합법적으로 바뀜 |
| 4~0 | M4~M0 | 위 1.3의 모드 코드 |

**SPSR(Saved PSR)**은 예외 진입 직전의 CPSR을 그대로 백업해두는 곳. User/System 모드에는 SPSR이 없음(예외 진입 전 상태이므로 애초에 필요 없음). 예외를 빠져나갈 때(`MOVS PC, LR` 같은 특수 형태) SPSR을 CPSR로 복원하면서 원래 모드로 돌아감.

### 1.5 파이프라인과 PC

fetch → decode → execute 3단계가 겹쳐서 동시에 돌아가는 파이프라이닝 구조. 그 부작용으로 명령어 실행 중 PC(R15)를 읽으면 "이 명령어 자신의 주소"가 아니라 앞서가 있는 값이 보인다:

- ARM 상태(명령어 4바이트): PC를 읽으면 `현재 명령어 주소 + 8`
- Thumb 상태(명령어 2바이트): PC를 읽으면 `현재 명령어 주소 + 4`
- 레지스터로 시프트량을 지정하는 데이터 프로세싱 인코딩에서 PC를 오퍼랜드로 쓰면 ARM 상태 기준 `+12`(내부적으로 사이클을 하나 더 먹기 때문)
- 분기(B/BL)의 타겟 주소 계산도 같은 오프셋 규칙을 그대로 적용
- 분기가 아닌 명령어는 실행 후 PC가 명령어 크기만큼(ARM=4, Thumb=2) 자동 증가. 분기 명령어는 PC를 직접 덮어쓰므로 이 자동 증가를 건너뜀

### 1.6 명령어 인코딩 — ARM 상태

32비트 고정 크기. 최상위 4비트(`instr[31:28]`)는 항상 조건 코드(아래 1.7), 그다음 `instr[27:25]` 3비트로 큰 카테고리를 가른다:

| 카테고리 | 대표 니모닉 | 비고 |
|---|---|---|
| 데이터 프로세싱 / PSR Transfer | MOV/ADD/SUB/CMP/AND/ORR ... / MRS/MSR | 오퍼랜드2를 즉시값 회전 또는 배럴 시프터로 계산 |
| 곱셈 / 곱셈-누산 | MUL/MLA, MULL/MLAL(64비트 결과 버전) | 데이터 프로세싱과 비트패턴이 겹쳐서 별도 판별 필요 |
| 단일 데이터 전송 | LDR/STR(바이트/워드) | 베이스+오프셋 주소 계산, 프리/포스트 인덱싱 |
| 하프워드/부호있는 데이터 전송 | LDRH/STRH/LDRSB/LDRSH | 16비트 단위 또는 부호확장 로드 |
| 블록 데이터 전송 | LDM/STM | 레지스터 여러 개를 한 번에, 스택 push/pop의 실제 구현체 |
| 분기 | B/BL | 24비트 워드 오프셋 |
| 분기-교환 | BX | ARM↔Thumb 전환, T비트를 바꾸는 유일한 합법적 명령어 |
| 소프트웨어 인터럽트 | SWI | BIOS 함수 호출(10번 참고) |
| 코프로세서 관련 | CDP/MRC/MCR/LDC/STC | GBA엔 코프로세서가 없어 실질적으로 미사용(NDS ARM9의 CP15가 이 인코딩을 씀) |
| 미정의 | — | 위 어디에도 안 맞으면 Undefined 예외 발생 |

### 1.7 조건 코드 (Condition Codes)

ARM의 특징: **거의 모든 명령어에 조건을 걸 수 있다**(x86처럼 "비교 후 분기"가 기본이 아님). 최상위 4비트가 아래 15가지 중 하나를 나타내고, CPSR의 N/Z/C/V와 안 맞으면 그 명령어 자체를 실행하지 않고 건너뜀(파이프라인만 소모):

| 코드 | 니모닉 | 조건 |
|---|---|---|
| 0000 | EQ | Z=1 (같음) |
| 0001 | NE | Z=0 (다름) |
| 0010 | CS/HS | C=1 (부호없는 ≥) |
| 0011 | CC/LO | C=0 (부호없는 <) |
| 0100 | MI | N=1 (음수) |
| 0101 | PL | N=0 (양수 또는 0) |
| 0110 | VS | V=1 (오버플로우) |
| 0111 | VC | V=0 |
| 1000 | HI | C=1 AND Z=0 (부호없는 >) |
| 1001 | LS | C=0 OR Z=1 (부호없는 ≤) |
| 1010 | GE | N==V (부호있는 ≥) |
| 1011 | LT | N!=V (부호있는 <) |
| 1100 | GT | Z=0 AND N==V (부호있는 >) |
| 1101 | LE | Z=1 OR N!=V (부호있는 ≤) |
| 1110 | AL | 항상 실행(조건 없음, 사실상 기본값) |
| 1111 | (예약) | ARM7TDMI에선 미사용 |

### 1.8 배럴 시프터 (Barrel Shifter)

데이터 프로세싱 명령어의 두 번째 오퍼랜드(operand2)를 만드는 전용 하드웨어. 별도 사이클 소모 없이 시프트 연산을 ALU 앞단에서 끼워 처리한다. 두 갈래:

- **즉시값(Immediate)**: 8비트 값을 0~30 짝수 칸만큼 ROR(오른쪽 회전)해서 32비트로 확장. 이렇게 하면 적은 비트로도 다양한 32비트 상수(특히 회전된 패턴)를 표현 가능
- **레지스터 + 시프트**: 레지스터 값을 아래 4종류 시프트 중 하나로 가공
  - **LSL(Logical Shift Left)**: 왼쪽으로 밀고 빈 자리는 0
  - **LSR(Logical Shift Right)**: 오른쪽으로 밀고 빈 자리는 0
  - **ASR(Arithmetic Shift Right)**: 오른쪽으로 밀되 빈 자리는 부호비트로 채움(부호 유지 나눗셈 효과)
  - **ROR(Rotate Right)**: 오른쪽으로 밀려 빠진 비트가 반대쪽(왼쪽)으로 순환

시프트량은 즉시값(5비트) 또는 레지스터 하위 8비트로 지정 가능. 시프트 중 마지막에 밀려난 비트가 `캐리 아웃(shifter carry out)`이 되어, S비트가 설정된 논리 연산(MOV/AND/ORR 등)의 C플래그를 갱신하는 데 쓰임(산술 연산의 캐리는 이것과 무관하게 ALU 자체의 덧셈/뺄셈에서 나옴).

### 1.9 Thumb 명령어 세트

16비트 압축 명령어. ARM 명령어의 부분집합을 재인코딩한 것이라 표현 가능한 범위가 좁아지지만(예: 조건부 실행이 분기 명령어 하나만 가능, 레지스터도 R0~R7 위주), 코드 밀도가 높아서(같은 기능이 더 적은 바이트) 16비트 폭의 GBA ROM/WRAM 버스에서 페치가 더 빠르다 — 그래서 GBA 상용 게임 대부분이 Thumb 위주로 컴파일된다.

- 총 19가지 명령어 포맷으로 분류(포맷별로 상위 몇 비트 패턴이 다름)
- 하이 레지스터(R8~R15) 접근은 일부 명령어(MOV/ADD/CMP/BX)에서만 제한적으로 허용
- `BL`은 Thumb에서 2개의 16비트 명령어(오프셋 상위/하위)로 나뉘어 인코딩됨 — 단일 32비트 명령어가 없어서 생기는 절충
- ARM 상태로 되돌아갈 때도 `BX` 사용(T비트를 0으로)

### 1.10 예외 / 인터럽트 벡터 테이블

주소 `0x00000000`~`0x0000001C`(BIOS 영역 시작 부분)에 8개의 고정 벡터가 있고, 예외 발생 시 해당 모드로 전환되며 이 주소로 강제 점프한다:

| 주소 | 예외 | 진입 모드 |
|---|---|---|
| `0x00000000` | Reset | Supervisor |
| `0x00000004` | Undefined Instruction | Undefined |
| `0x00000008` | Software Interrupt (SWI) | Supervisor |
| `0x0000000C` | Prefetch Abort | Abort |
| `0x00000010` | Data Abort | Abort |
| `0x00000014` | (예약) | — |
| `0x00000018` | IRQ | IRQ |
| `0x0000001C` | FIQ | FIQ |

예외 진입 시 하드웨어가 자동으로 하는 일: ① 현재 CPSR을 해당 모드의 SPSR에 백업 ② 복귀 주소를 LR에 저장 ③ CPSR의 모드 비트를 바꾸고 I비트(및 FIQ면 F비트도) 설정해서 같은 예외 중첩 방지 ④ 벡터 주소로 점프. GBA는 이 8개 벡터 자리에 실제로는 전부 BIOS의 공용 디스패치 코드가 있고, IRQ의 경우 그 코드가 다시 사용자 등록 핸들러 주소(`0x03007FFC`)로 점프하는 2단 구조를 쓴다(3번 인터럽트 섹션 참고).

### 1.11 사이클 타이밍

명령어마다 실행에 걸리는 사이클 수가 다르며, 실사용 에뮬레이터에서는 이 차이를 재현해야 사운드/타이머/DMA가 실제 하드웨어와 어긋나지 않는다:

- 기본 데이터 프로세싱: 보통 1S(1 Sequential cycle, 순차 메모리 접근 1회분) — 단, PC를 목적지로 쓰면 파이프라인이 새로 채워져야 해서 추가 사이클(2S+1N) 소모
- 레지스터 시프트를 쓰는 데이터 프로세싱: +1I(Internal cycle, 내부 연산 사이클) 추가
- 곱셈(MUL/MLA): 오퍼랜드 크기에 따라 1~4 사이클 가변(상위 바이트가 전부 0이거나 전부 1이면 더 빨리 끝나는 최적화 로직이 실제 하드웨어에 있음 — "m" 사이클 계산 규칙)
- 메모리 접근(LDR/STR/LDM/STM): 접근하는 메모리 영역의 웨이트스테이트(2번 섹션)가 그대로 추가됨, LDM/STM은 레지스터 개수만큼 사이클 누적
- 분기(B/BL/BX): 파이프라인을 다시 채워야 해서 2S+1N (약 3사이클) 소모

용어: **S-cycle(Sequential)**은 직전 접근과 이어지는 주소로의 접근(더 빠름), **N-cycle(Non-sequential)**은 완전히 새로운 주소로의 접근(더 느림), **I-cycle(Internal)**은 메모리 접근 없이 CPU 내부에서만 도는 사이클.

## 2. 메모리 맵

| 주소 범위 | 이름 | 크기 | 설명 |
|---|---|---|---|
| `0x00000000`–`0x00003FFF` | BIOS | 16KB | 부팅 코드 + SWI 핸들러. 실행 중엔 읽기 불가(보안) — 이 때문에 에뮬레이터는 실제 BIOS 덤프 없이 SWI를 흉내내는 **HLE(High-Level Emulation, 실제 코드 대신 그 코드의 "결과"만 직접 구현)** 방식을 쓰기도 함 |
| `0x02000000`–`0x0203FFFF` | EWRAM (외장 WRAM) | 256KB | External Work RAM. 느리지만 큼(버스 폭 16비트) |
| `0x03000000`–`0x03007FFF` | IWRAM (내장 WRAM) | 32KB | Internal Work RAM. 칩 내장이라 빠름(32비트 버스, 0웨이트). 스택/자주 쓰는 변수가 여기 배치되는 게 보통 |
| `0x04000000`–`0x040003FE` | I/O 레지스터 | ~1KB | 하드웨어 제어 레지스터 전부 |
| `0x05000000`–`0x050003FF` | 팔레트 RAM | 1KB | BG용 256색 + 스프라이트용 256색, 각각 15비트 컬러 |
| `0x06000000`–`0x06017FFF` | VRAM | 96KB | 타일/비트맵 그래픽 데이터 |
| `0x07000000`–`0x070003FF` | OAM | 1KB | Object Attribute Memory, 스프라이트 128개의 속성(위치/크기/타일번호/우선순위 등) |
| `0x08000000`–`0x09FFFFFF` | Game Pak ROM (WS0) | 최대 32MB | 카트리지 롬, 웨이트스테이트 0 설정으로 접근했을 때의 미러 |
| `0x0A000000`–`0x0BFFFFFF` | Game Pak ROM (WS1) | 〃 | 같은 ROM의 미러, 웨이트스테이트 1 설정 |
| `0x0C000000`–`0x0DFFFFFF` | Game Pak ROM (WS2) | 〃 | 같은 ROM의 미러, 웨이트스테이트 2 설정 |
| `0x0E000000`–`0x0E00FFFF` | Game Pak SRAM/Flash/EEPROM | 최대 64KB(SRAM 기준) | 세이브 데이터 영역 |

**웨이트스테이트(Wait State)**: 메모리 영역마다 CPU가 접근할 때 기다려야 하는 추가 사이클 수가 다르다. IWRAM은 0웨이트(즉시), EWRAM은 2웨이트, ROM은 `WAITCNT`(`0x04000204`) 레지스터 설정에 따라 가변(1~4사이클, N-cycle/S-cycle 구분: 새 주소로 처음 접근하는 Non-sequential이 더 느리고, 이어서 순차 접근하는 Sequential은 더 빠름). **이걸 무시하고 만들면 게임이 돌아가긴 해도 타이밍이 어긋나서 사운드가 밀리거나 프레임이 안 맞는 문제가 생김** — 실사용 에뮬레이터에서 꼭 구현해야 하는 부분.

## 3. 인터럽트

- `IE`(`0x04000200`, Interrupt Enable) — 어떤 인터럽트 종류를 받을지 비트마스크로 설정
- `IF`(`0x04000202`, Interrupt Flag/Request) — 실제로 발생한 인터럽트 표시(쓰기로 1을 넣으면 해당 비트가 클리어되는 특이한 방식, "write-1-to-clear")
- `IME`(`0x04000208`, Interrupt Master Enable) — 전체 인터럽트 on/off 스위치
- 인터럽트 종류(비트 순서): VBlank, HBlank, VCount match, Timer0~3, Serial, DMA0~3, Keypad, Game Pak(카트리지 분리 감지)
- 인터럽트 발생 시 CPU는 IRQ 모드로 전환되고 고정 벡터(`0x00000018`)로 점프 → 거기서 BIOS의 공용 핸들러가 사용자 정의 핸들러 주소(`0x03007FFC`에 게임이 미리 등록해둠)로 다시 점프하는 2단 구조

## 4. 타이머

- 4개(Timer0~3), 각각 `TMxCNT_L`(카운터 값, 16비트 다운카운트 아닌 업카운트 후 오버플로우 방식), `TMxCNT_H`(제어: 프리스케일러, 카스케이드, IRQ enable, start/stop)
- **프리스케일러**: CPU 클럭을 1/1, 1/64, 1/256, 1/1024로 나눠서 카운트 속도 조절
- **카스케이드(Cascade)**: 이전 타이머가 오버플로우할 때만 다음 타이머가 1 증가 — 이렇게 연결하면 32비트 타이머처럼 쓸 수 있음
- 사운드의 Direct Sound 재생 속도 제어에도 타이머가 쓰임(7번 사운드 섹션 참고)

## 5. DMA

- 4채널(DMA0~3), 우선순위는 채널 번호가 낮을수록 높음
- 레지스터: `DMAxSAD`(Source Address), `DMAxDAD`(Destination Address), `DMAxCNT_L`(전송 개수), `DMAxCNT_H`(제어: 전송단위 16/32비트, 반복 여부, 타이밍)
- **전송 타이밍 종류**: 즉시(Immediate), VBlank 시작 시, HBlank 시작 시, 특수(Special — 채널별로 다른 의미: DMA1/2는 사운드 FIFO 리필용, DMA3는 비디오 캡처용)
- DMA0은 내부 메모리끼리만, DMA1~3은 카트리지 ROM도 소스로 가능
- 사운드 FIFO(7번 참고)를 채우는 게 DMA1/DMA2의 가장 흔한 용도

## 6. 그래픽 (PPU)

`DISPCNT`(`0x04000000`, Display Control)의 하위 3비트로 **배경 모드(BG Mode) 0~5**를 고른다. 모드에 따라 배경 4개(BG0~3)의 성격이 완전히 달라짐:

| 모드 | 성격 | 사용 가능 배경 |
|---|---|---|
| 0 | 전부 텍스트(타일) 모드, 회전/확대 없음 | BG0~3 |
| 1 | BG0/1은 텍스트, BG2는 아핀 | BG0~2 |
| 2 | 전부 아핀(회전/확대) 모드 | BG2~3 |
| 3 | 비트맵, 240×160, 15비트 컬러 직접 저장(더블버퍼 불가) | BG2 |
| 4 | 비트맵, 240×160, 8비트 팔레트 인덱스(더블버퍼 가능) | BG2 |
| 5 | 비트맵, 160×128(작은 해상도), 15비트 컬러(더블버퍼 가능) | BG2 |

- **텍스트 모드(Text Mode)**: 화면을 8×8 타일(작은 그림 조각) 격자로 채우는 방식. 타일 번호를 담은 "타일맵(스크린 데이터)"과 실제 타일 그림 데이터("캐릭터 데이터")가 VRAM에 분리 저장됨
- **아핀 모드(Affine Mode)**: 회전/확대/축소 변환 행렬을 적용해서 배경이나 스프라이트를 그리는 방식. 레이싱 게임의 회전 트랙 등에 쓰임
- **비트맵 모드**: 타일 대신 픽셀을 직접 프레임버퍼처럼 다루는 방식. 구현은 쉽지만 메모리를 많이 먹고 스크롤이 느림(그래서 실제 게임에선 잘 안 씀)
- **스프라이트/OAM**: 배경과 별개로 움직이는 오브젝트(캐릭터 등). OAM에 위치/크기/타일번호/팔레트/우선순위/아핀 여부를 담은 속성 128개가 있고, 실제 그림 데이터는 VRAM의 별도 영역(오브젝트용 캐릭터 데이터)에서 가져옴
- **스캔라인 타이밍**: 화면은 위에서 아래로 한 줄(스캔라인)씩 그려짐. 한 줄 다 그리면 **HBlank**(수평 귀선 구간, 잠깐 쉬는 시간 — 이때 다음 줄 준비를 위한 DMA/레지스터 갱신이 자주 일어남), 화면 전체(160줄) 다 그리면 **VBlank**(수직 귀선 구간 — 게임 로직이 다음 프레임 그리기 전 VRAM/OAM을 안전하게 갱신하는 타이밍으로 씀). `VCOUNT`(`0x04000006`)가 지금 그리고 있는 스캔라인 번호, `DISPSTAT`(`0x04000004`)가 HBlank/VBlank 상태 플래그를 담음
- 스캔라인 단위로 정확히 타이밍을 맞춰 렌더링해야(스캔라인 레벨 에뮬레이션) 레이스터 효과(스캔라인마다 스크롤값을 바꿔서 파도/굴곡 효과를 내는 기법) 같은 게 제대로 나옴 — 프레임 단위로 통째로 그리면 이런 효과가 깨짐

## 7. 사운드

두 계통이 섞여 있다:

- **레거시 4채널(Game Boy 호환)**: 펄스(구형파) 2개 + 웨이브(사용자 정의 파형) 1개 + 노이즈 1개. 원래 Game Boy의 사운드 칩을 그대로 물려받은 부분이라 레지스터 구조도 옛날 GB 방식
- **Direct Sound (DMA 사운드) 2채널(A/B)**: 8비트 PCM(펄스 코드 변조 — 소리 파형을 숫자로 직접 저장한 방식) 샘플을 FIFO(선입선출 버퍼)에 넣고 타이머 주기에 맞춰 DMA로 계속 채워 넣으면서 재생. 게임 배경음악/음성 대부분은 이 방식(트래커 형식 음악을 소프트웨어로 믹싱해서 PCM으로 흘려보냄)
- 최종적으로 6채널(레거시 4 + Direct Sound 2)을 `SOUNDCNT_H`/`SOUNDCNT_X` 등에서 믹싱 비율/좌우채널 설정해서 하나의 출력으로 합침
- 에뮬레이터 구현 시 CPU 사이클과 오디오 샘플레이트를 동기화하는 리샘플링이 필요(사운드 끊김/피치 오류의 주요 원인)

## 8. 키패드 입력

- `KEYINPUT`(`0x04000130`) — 읽기 전용, **0이 눌림, 1이 안 눌림**(active-low)이라는 점 주의. A/B/Select/Start/방향키/L/R 총 10비트
- `KEYCNT`(`0x04000132`) — 특정 키 조합이 눌렸을 때 인터럽트를 발생시키는 설정(AND/OR 조건 선택 가능)

## 9. 시리얼 통신 (SIO) — 링크케이블

통신 기능을 목표로 하신다면 이 섹션이 핵심. `SIOCNT`(`0x04000128`)로 4가지 모드 중 하나를 고른다:

- **Normal 모드**: 8비트 또는 32비트 단순 시프트 레지스터 통신. 주로 GBA-GBC 통신이나 간단한 주변기기용
- **Multi-Player 모드**: 링크케이블로 최대 4대의 GBA를 연결하는 표준 멀티플레이 방식. 1대가 **부모(Parent)**, 나머지가 **자식(Child, 최대 3대)** 역할. 부모가 통신 클럭을 생성하고 주도권을 가짐. 데이터는 `SIOMULTI0`~`SIOMULTI3`(`0x04000120`~`0x04000126`)에 각 플레이어 값이 채워짐
  - 통신 절차: 부모가 시작 → 클럭에 맞춰 전원이 동시에 데이터 교환(부모→전체 브로드캐스트 + 각 자식→부모) → 완료 시 `SIOCNT`의 완료 플래그/인터럽트로 알림
  - **에뮬레이터 구현 이슈**: 실기에서는 물리적 케이블 전송 지연이 있지만, 에뮬레이터 두 인스턴스를 통신시키려면 두 CPU의 실행 타이밍을 어떻게든 맞춰야 함(한쪽이 너무 앞서가면 안 됨) — 이게 "통신 되는 에뮬레이터"를 만들 때 제일 까다로운 부분. 보통 통신 시점에서 양쪽을 동기화 지점(sync point)까지 나란히 진행시키는 방식으로 구현
- **UART 모드**: 표준 시리얼(RS-232 유사) 통신. 실사용 사례는 적음
- **JOY BUS 모드**: 게임큐브 등 다른 닌텐도 기기와 통신할 때 쓰는 특수 모드(`JOYCNT`/`JOY_RECV`/`JOY_TRANS`/`JOYSTAT`, 주소 `0x04000140` 이후)
- `RCNT`(`0x04000134`) — SIO 모드 자체를 일반 GPIO(범용 입출력 핀)처럼 쓸 수 있게 하는 레지스터. 일부 주변기기(예: 마이크, 특수 카트리지)가 이 방식을 씀
- **멀티부트(Multiboot)**: 카트리지 없이 링크케이블만으로 작은 프로그램(최대 256KB)을 상대 GBA에 전송해서 실행시키는 기능. 부모 쪽 BIOS의 멀티부트 SWI가 이 프로토콜을 처리. "친구가 카트리지 없이도 미니게임에 참여" 기능의 원리가 이것

## 10. BIOS / SWI 함수

게임 코드는 나눗셈처럼 흔한 연산도 직접 계산하지 않고 `SWI`(Software Interrupt, 소프트웨어 인터럽트 — CPU 예외를 일부러 발생시켜서 BIOS의 특정 루틴을 호출하는 방식) 명령어로 BIOS 함수를 호출하는 경우가 많다. 대표적인 것들:

- `Div`/`DivArm` — 나눗셈(ARM7TDMI엔 나눗셈 명령어가 없어서 소프트웨어로 구현됨)
- `Sqrt` — 정수 제곱근
- `ArcTan`/`ArcTan2` — 삼각함수 역함수(회전 계산용)
- `CpuSet`/`CpuFastSet` — 메모리 복사/채우기(후자는 32바이트 단위 고속 버전)
- `LZ77UnCompVram`/`HuffUnCompVram`/`RLUnCompVram` — 압축 해제(카트리지 용량 절약을 위해 그래픽/데이터를 압축해서 넣고 실행 시 푸는 용도)
- `VBlankIntrWait` — VBlank 인터럽트가 올 때까지 대기(프레임 동기화의 표준 방법)
- **HLE vs LLE**: 실제 BIOS ROM 덤프를 넣고 진짜로 그 코드를 실행시키는 게 LLE(Low-Level Emulation), 대신 "이 SWI 번호가 호출되면 이런 결과를 낸다"를 직접 C++ 등으로 구현해서 대체하는 게 HLE. HLE가 더 빠르고 저작권 이슈(BIOS ROM은 닌텐도 저작물)도 피할 수 있지만, 일부 까다로운 게임은 LLE가 아니면 미묘한 타이밍 차이로 오동작하기도 함

## 11. 카트리지 세이브

카트리지마다 세이브 메모리 종류가 다르고, **주소로는 구분이 안 되고 ROM 안의 문자열 시그니처로 감지**해야 한다(실무 팁):

| 종류 | 크기 | 감지 문자열(ROM 내 탐색) | 접근 방식 |
|---|---|---|---|
| SRAM | 32KB/64KB | `"SRAM_"` | 일반 메모리처럼 직접 읽기/쓰기 |
| Flash | 64KB/128KB | `"FLASH_"`, `"FLASH512_"`, `"FLASH1M_"` | 뱅크 전환 + 커맨드 시퀀스 필요(제조사별 칩 프로토콜 차이 있음, Panasonic/Sanyo 등) |
| EEPROM | 512B/8KB | `"EEPROM_"` | 시리얼 비트 단위 프로토콜(DMA로 접근해야 함 — 일반 메모리 접근 방식이 아님) |

## 12. 카트리지 헤더

ROM 맨 앞(`0x08000000`)에 고정 포맷 헤더가 있다:

- `0x00`~`0x03`: 시작 점프 명령어(ARM 분기)
- `0x04`~`0x9F`: 닌텐도 로고 비트맵(BIOS가 부팅 시 이걸 체크섬으로 검증 — 여기가 틀리면 부팅 안 됨, 에뮬레이터도 이 체크를 흉내내거나 우회해야 함)
- `0xA0`~`0xAB`: 게임 타이틀(12바이트)
- `0xAC`~`0xAF`: 게임 코드(4바이트, 예: 지역/게임 식별)
- `0xB2`: 고정값 `0x96`
- `0xBD`: 헤더 체크섬(단순 합산 검증)

## 13. 에뮬레이터 설계 실무

지식이라기보다 "실제로 동작하는 에뮬레이터"를 만들 때 부딪히는 설계 결정들:

- **Cycle-accurate vs Instruction-accurate**: 명령어 하나 실행 후 사이클 수만큼 다른 컴포넌트(PPU/APU/타이머)도 같이 전진시키느냐(cycle-accurate, 정확하지만 느림), 아니면 명령어 단위로 뭉텅이로 처리하고 나중에 보정하느냐(instruction-accurate, 빠르지만 일부 타이밍 의존 게임에서 오차)의 트레이드오프
- **스케줄러(Scheduler)**: CPU/PPU/APU/타이머/DMA가 전부 "동시에" 돌아가야 하는데 실제 구현은 순차 코드이므로, 이벤트 기반 스케줄러(다음에 어떤 컴포넌트가 언제 상태를 바꿔야 하는지 큐로 관리)를 두는 게 일반적인 아키텍처
- **통신 동기화**: 9번에서 언급한 대로, 두 에뮬레이터 인스턴스(또는 다른 유저와 네트워크로) 통신을 구현하려면 SIO 이벤트 시점에서 양쪽 실행을 맞추는 동기화 지점이 필요. 네트워크 지연이 있는 경우 롤백/버퍼링 전략도 고려 대상
- **저장 상태(Save State)**: 세이브 파일과 별개로 에뮬레이터 자체 상태(레지스터/메모리 전체)를 스냅샷 저장하는 기능 — 실사용 에뮬레이터라면 사실상 필수 기능으로 꼽힘
- **입력 지연(Input Lag)**: 프레임 타이밍/오디오 버퍼링과 얽혀서 체감 반응성에 영향을 줌. 실사용을 목표로 한다면 신경 써야 할 부분

## 14. 용어집 (Glossary)

| 용어 | 뜻 |
|---|---|
| ARM7TDMI | GBA에 쓰인 32비트 RISC CPU 코어. T=Thumb지원, D=디버그, M=곱셈기, I=임베디드ICE(디버깅 회로) |
| BIOS | Basic Input/Output System. 부팅 및 저수준 시스템 함수(SWI)를 담은 내장 ROM |
| WRAM | Work RAM, 작업용 램. GBA는 내장(IWRAM, 빠름)과 외장(EWRAM, 느림) 두 종류가 있음 |
| VRAM | Video RAM, 그래픽 데이터 전용 메모리 |
| OAM | Object Attribute Memory, 스프라이트 속성 정보를 담은 메모리 영역 |
| 웨이트스테이트(Wait State) | 메모리 접근 시 추가로 기다려야 하는 CPU 사이클 |
| DMA | Direct Memory Access. CPU 개입 없이 메모리 간 데이터를 자동으로 옮기는 하드웨어 기능 |
| HBlank / VBlank | 화면을 그리는 중간에 생기는 수평/수직 "쉬는 시간" 구간. 안전하게 그래픽 데이터를 갱신할 타이밍으로 활용됨 |
| PPU | Picture Processing Unit, 화면 그리기를 담당하는 회로(GBA엔 공식 명칭 없이 관례적으로 이렇게 부름) |
| 아핀(Affine) | 회전/확대/축소/기울임 변환. 배경이나 스프라이트에 적용 가능 |
| 텍스트 모드(Text Mode) | 8×8 타일을 격자로 배치해서 화면을 구성하는 배경 그리기 방식 |
| SIO | Serial I/O, 시리얼(직렬) 통신 하드웨어. 링크케이블 통신의 기반 |
| 멀티부트(Multiboot) | 카트리지 없이 링크케이블로 작은 프로그램을 전송해 실행시키는 기능 |
| SWI | Software Interrupt, 소프트웨어가 의도적으로 발생시키는 예외. BIOS 함수 호출에 쓰임 |
| HLE / LLE | High/Low-Level Emulation. BIOS를 직접 실행하지 않고 결과만 흉내내는 방식(HLE) vs 실제 코드를 그대로 실행하는 방식(LLE) |
| PCM | Pulse Code Modulation, 소리 파형을 숫자 샘플로 직접 표현하는 방식 |
| FIFO | First-In-First-Out, 먼저 들어온 데이터가 먼저 나가는 버퍼 구조. Direct Sound 재생에 사용 |
| 프리스케일러(Prescaler) | 원래 클럭을 일정 비율로 나눠서 더 느린 카운트 속도를 만드는 설정 |
| 카스케이드(Cascade) | 타이머 등을 연쇄로 연결해서 앞 단계가 넘칠 때만 다음 단계가 증가하는 연결 방식 |
| 스케줄러(Scheduler) | 여러 하드웨어 컴포넌트의 타이밍을 이벤트 단위로 조율하는 에뮬레이터 내부 구조 |
| 세이브 스테이트(Save State) | 게임 자체 세이브가 아니라 에뮬레이터가 그 순간의 전체 상태를 저장/복원하는 기능 |
| RISC | Reduced Instruction Set Computer, 명령어를 단순화해 하나하나를 빠르게 처리하는 CPU 설계 철학 |
| 뱅킹(Register Banking) | CPU 모드가 바뀔 때 R8~R14 일부가 그 모드 전용 사본으로 자동 교체되는 것 |
| CPSR / SPSR | Current/Saved Program Status Register. CPSR=지금 CPU 상태, SPSR=예외 진입 직전 상태의 백업 |
| N/Z/C/V 플래그 | Negative/Zero/Carry/Overflow. 연산 결과 특성을 나타내는 CPSR 상위 4비트, 조건부 실행 판정에 쓰임 |
| 배럴 시프터(Barrel Shifter) | 별도 사이클 없이 시프트 연산을 ALU 앞단에서 처리하는 전용 하드웨어 |
| S-cycle / N-cycle / I-cycle | Sequential(이어지는 주소 접근, 빠름) / Non-sequential(새 주소 접근, 느림) / Internal(메모리 접근 없는 내부 연산) 사이클 |
| 하이 레지스터(Hi Register) | Thumb 명령어에서 접근이 제한적으로만 허용되는 R8~R15 |
